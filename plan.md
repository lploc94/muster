# Batch C: SQLite privacy, recovery guidance, and packaged fault closeout

## Target
Deliver P5-W6 and P5-W7 as two resumable commits: enforce and document the SQLite privacy/recovery contract, then prove the complete Phase 5 behavior through redacted packaged Extension Host fault UAT on minimum and current VS Code runtimes and close Phase 5 only after every regression gate passes.

## Source Of Truth
- Plan schema: `loop-plan/v1`
- Request/report: Plan Batch C (`P5-W6` and `P5-W7`) from `docs/plans/sqlite-global-storage-refactor.vi.md:928-936,1058-1095`; Batch A and Batch B are completed baseline work and must not be reimplemented.
- Baseline commit: `036d740`
- Plan path: `plan.md`

## Current State
- Completed: Phase 4 is closed. Commits `86dbc73`, `01506b2`, and `777c900` completed P5-W1 through P5-W3; `a897014`, `8923b86`, and `036d740` completed and hardened P5-W4/W5. The current runtime is SQLite-only; safe fixed-code worker errors, fail-closed open, durable-write rollback, SQLite-aware backup, explicit in-place reset, maintenance hold/quiesce, and peer revision-regression recovery already exist and must be reused.
- Remaining: P5-W6 has no dedicated secret-canary audit suite, SQLite storage/recovery guide, docs verifier, or comprehensive sink/static guard for the privacy contract. P5-W7 has no packaged fault-UAT matrix for corrupt/foreign/incompatible open, injected full/readonly rollback, busy responsiveness, concurrent-WAL backup, reset cancel/success, or two-window reset contention; there is no Phase 5 evidence ledger/verifier, and Phase 5 is not marked complete.
- Worktree: clean on `main` at discovery (`HEAD 036d740`, 35 commits ahead of `origin/main`); writing this planner-owned `plan.md` makes only that tracked file dirty. Preserve the local history, unrelated `/private/tmp/muster-pr19-review` worktree, and any unrelated changes that appear during implementation.

## Codebase Evidence
- Current flow: `src/extension.ts:activate -> registerSqliteMaintenanceCommands -> DbClient.open -> src/task/sqlite/worker.ts:handle -> openStoreDatabase -> WorkspaceRegistry.getOrCreate -> SqliteTaskRepository -> TaskEngine.loadAsync`; runtime writes and maintenance backup/reset remain worker-owned, while errors cross `serializeMusterError -> DbClient -> diagnoseSqliteError/redactedDiagnosticLogFields -> fixed command UI/debugMuster`. The packaged happy-path flow is `scripts/run-sqlite-extension-host-smoke.mjs -> fresh VSIX -> scripts/sqlite-extension-host-smoke.ts:run -> packaged DbClient/worker`, and the live multi-process pattern is `scripts/run-sqlite-two-window-live-uat.mjs -> two VS Code processes -> scripts/sqlite-two-window-live-uat.ts -> UAT_COMMANDS`.
- Existing tests: `src/task/sqlite/errors.test.ts`, `diagnostics.test.ts`, and `protocol.test.ts` prove fixed redacted worker/host payloads; `connection.test.ts`, `write-failure.test.ts`, `backup.test.ts`, and `reset.test.ts` prove fail-closed fixtures, rollback, WAL-aware snapshots, and atomic reset; `src/host/sqlite-maintenance-commands.test.ts` proves safe command results/cancellation; `scripts/source-boundary-smoke*.mjs` and `repository-boundary-smoke*.mjs` reject legacy/raw-copy paths; `scripts/sqlite-extension-host-smoke.ts` proves packaged schema and happy backup; Phase 4's two-window runner and `sqlite-phase4-two-window-live-uat-evidence.json` prove real shared-globalStorage hosts but do not exercise Phase 5 faults.
- Pattern to follow: `src/task/sqlite/errors.ts:safeMessageForCode/serializeMusterError`, `src/task/sqlite/diagnostics.ts:redactedDiagnosticLogFields`, and `src/host/sqlite-maintenance-commands.ts:handleBackupDatabaseCommand/handleDeveloperResetCommand` are the redaction-by-construction pattern; `scripts/verify-task-export-docs.test.mjs:validate` is the tracked-doc marker/forbidden-claim pattern; `src/host/uat-commands.ts:isUatModeEnabled` plus `src/extension.ts:registerLiveUatCommands` is the non-production UAT seam; `scripts/run-sqlite-extension-host-smoke.mjs` and `scripts/run-sqlite-two-window-live-uat.mjs` are the fresh-VSIX and two-real-host launch patterns; `scripts/verify-live-uat-evidence.test.mjs` is the redacted evidence verifier pattern.
- Pattern fallback: None.
- Verified gap: The current redaction foundation is broad but there is no one test that stores a unique canary and proves its allowed/forbidden destinations, no user/developer document covers `globalStorageUri` scope/WAL/backup-versus-export/reset/recovery/no-at-rest-encryption, and current packaged smoke covers only normal open/schema/backup. Unit tests cannot satisfy P5-W7's explicit minimum/stable packaged and cross-window fault evidence requirement.

## Scope
- In scope: P5-W6 canary-driven audit of SQLite RPC/errors/diagnostics/command results/output/evidence metadata; static guards against raw SQLite content/path/params, telemetry/content sinks, legacy JSON/import compatibility, and unsafe evidence; user/developer storage/recovery documentation with executable assertions; P5-W7 packaged minimum/stable fault scenarios, real two-host reset contention, evidence schema/verifier/ledger, Phase 4 benchmark and live-UAT regression gates, source re-audit, and truthful Phase 5 completion markers.
- Out of scope: restore/import implementation, migration/backward compatibility, JSON fallback, a telemetry framework, encryption/SQLCipher, new performance architecture, activation `VACUUM`, raw copying or unlinking the live main/WAL/SHM trio, automatic reset, changes to Markdown export behavior, reimplementation of W1-W5, or Phase 6 virtualization/cleanup.

## Invariants
- `context.globalStorageUri/muster.sqlite3` remains the only durable runtime source for task/conversation data within one profile + extension-host authority; VS Code settings and SecretStorage remain separate.
- Production errors, diagnostics, logs, command results, change-feed metadata, non-content snapshot metadata, and published Phase 5 evidence contain only fixed codes, bounded counts/timing/hash/size, and other explicitly allowlisted non-sensitive fields—never raw SQL/params, stack, paths/URIs, workspace/task/session IDs, cursor, credentials, prompt/tool output, or the secret canary. Non-production UAT control messages may carry synthetic opaque entity IDs required to drive a scenario, but those IDs and control payloads must not be copied into Phase 5 evidence.
- The canary may appear only in intentionally persisted conversation content and a user-initiated SQLite backup of that content; tests must not mistake intended transcript rendering for metadata leakage and must never commit the canary in evidence.
- No telemetry framework/content sink, host-thread production SQLite access, legacy filesystem JSON/importer, migration/backcompat, silent reset, malformed-row skip, swallowed busy error, raw live-WAL copy, or unbounded hydration is introduced.
- Fault injection remains explicit test/UAT-only capability, is unavailable in production mode/settings/commands, and every injected failure remains fixed-code, redacted, fail-closed, and atomic with no false ACK/revision or silent row loss.
- Backup/reset continue using the verified W4/W5 worker paths; packaged UAT must exercise those real paths rather than duplicate them with test-only storage implementations.
- Evidence is generated only from a freshly packaged VSIX and contains the allowlisted schema; paths, workspace identity, content, cursors, SQL, stacks, session IDs, and raw exceptions are rejected before publication.
- Batch C must not change the canonical SQLite schema: `src/task/sqlite/schema.ts` (`SQLITE_SCHEMA_VERSION` and `CURRENT_SCHEMA_STATEMENTS`) and `src/task/sqlite/schema-fingerprint.ts` remain byte-for-byte unchanged from baseline `036d740`.

## Global Gates
| Gate | Command | Expected result |
|---|---|---|
| Types | `npm run compile` | TypeScript and webview build pass |
| Static/UI checks | `npm run check:svelte` | 0 errors (no new warnings) |
| Build | `npm run vscode:prepublish` | Fresh packaged-source build passes |
| Full tests | `npm test` | All Vitest suites pass |
| Source boundaries/fixtures | `npm run test:source-boundary && npm run test:source-boundary:fixtures && npm run test:sqlite-storage-docs && npm run test:sqlite-phase5-evidence && git diff --exit-code 036d740 -- src/task/sqlite/schema.ts src/task/sqlite/schema-fingerprint.ts && git diff --check` | Production/fixture boundaries, docs contract, evidence schema/redaction, canonical schema baseline, and whitespace checks pass |

## Execution Rules
- Execute phases in order unless a phase explicitly declares no dependency.
- For each phase: revalidate the cited code evidence, write or update contract tests first, confirm the intended failure when feasible, evaluate whether the tests reject naive shortcuts, implement the full behavioral contract using project patterns, run focused tests, run all phase gates, review the uncommitted diff, fix findings, and commit.
- Green tests alone do not complete a phase. The production diff must satisfy every implementation obligation and invariant for the full described input/state class.
- Never commit with failing gates or an unapproved implementation review.
- Preserve unrelated user changes; never reset or revert them.
- Mark a phase complete in this plan in the same commit as that phase's implementation.
- Continue automatically to the next incomplete phase. Stop only for a destructive/irreversible decision, missing credentials/infrastructure, irreconcilable requirements, or repeated failures with no new evidence.

## Plan Review
- Status: APPROVE
- Rounds: 5 (3 in the initial session, 2 in the final full-draft session)
- Open issues: None

## Phase 1: P5-W6 SQLite privacy, redaction, and recovery contract
- Status: complete
- Depends on: None
- Goal: Make the privacy boundary executable and user-visible by proving the secret-canary allowlist, guarding every SQLite sink against regression, and documenting the actual global-storage, backup/export, reset, recovery, and at-rest privacy behavior.
- Current behavior: W1-W5 serialize fixed SQLite errors and use redacted diagnostics/command messages, but coverage is distributed across suites and generic `/secret/` fixtures. There is no end-to-end canary allowlist test, no comprehensive static guard over output/evidence/telemetry sinks, and no storage/recovery guide or verifier matching the two contributed command IDs.
- Code evidence: `src/task/sqlite/errors.ts:SafeSerializedDbError/serializeMusterError`; `src/task/sqlite/diagnostics.ts:diagnoseSqliteError/redactedDiagnosticLogFields/recoveryGuidanceFor`; `src/task/sqlite/client.ts:sanitizeFatal/onMessage`; `src/task/sqlite/rpc.ts:serializeError`; `src/host/sqlite-maintenance-commands.ts:BackupCommandResult/ResetCommandResult`; `src/extension.ts:debugMuster/registerSqliteMaintenanceCommands`; `src/task/repository.ts:WorkspaceChangeFeedResult/SqliteTaskRepository.getWorkspaceChangesSince`; `src/host/external-workspace-reconciler.ts`; `src/task/change-feed.test.ts`; `src/host/uat-commands.ts:readRedactedDbIdentity`; `scripts/repository-boundary-smoke.mjs`; `scripts/source-boundary-smoke.mjs`; `scripts/verify-task-export-docs.test.mjs`; `package.json:contributes.commands`.
- Pattern to follow: Extend redaction by construction through the fixed DTO helpers above; add negative fixtures to the existing boundary-smoke test pattern; add `docs/SQLITE-STORAGE.md` and a `scripts/verify-sqlite-storage-docs.test.mjs` validator following `verify-task-export-docs.test.mjs`, then link it through `docs/README.md`, root `README.md`, and the contributor verification guide.
- Behavioral contract:
  - A unique runtime-generated canary stored as user conversation content is readable from the durable conversation row and from an independently reopened user-requested W4 backup, but is absent from serialized worker errors, fatal details, diagnostics/log fields, maintenance command return/UI payloads, change-feed metadata, redacted DB identity, non-content snapshot/page metadata, and serialized UAT/evidence records.
  - Error/fault variants for open, read, write/transaction, backup, and reset preserve only the W1 fixed `{name, code, operation, message, kind}` contract and fixed host recovery fields. Output-channel/debug and notification calls receive allowlisted redacted objects/messages; direct success UI may show only the basename explicitly selected by the user, never a logged path.
  - Production source exposes no telemetry mechanism for SQLite data. Static checks fail if SQLite/maintenance code introduces telemetry, raw exception/stack, SQL/params, path/URI, conversation content, workspace/task identity, or canary-bearing payloads into logging, diagnostics, evidence, or command-result metadata, and continue rejecting `.muster-tasks.json`, filesystem importer, migration, or compatibility paths.
  - `docs/SQLITE-STORAGE.md` states that storage is one database per VS Code profile + extension-host authority under `globalStorageUri`, is not repository-local or shared across authorities/profiles, and coordinates `muster.sqlite3`, `-wal`, and `-shm`. It distinguishes task-scoped Markdown export from whole-database backup, documents exact commands `muster.backupDatabase` and `muster.developerResetGlobalDatabase`, and distinguishes the supported manual restore procedure from the absence of an in-product restore/import command.
  - The guide documents backup-before-reset, global destructive scope, cancellation/failure semantics, close-other-window contention, and recovery for corrupt/not-a-database, incompatible/foreign, full, readonly, I/O, and busy cases. Manual restore requires closing every Muster window for that profile/authority, preserving or moving the existing main/WAL/SHM trio together, placing the independently usable backup as `muster.sqlite3` without pairing it with stale sidecars, and reopening Muster to run normal ownership/schema checks; it never advises replacing an open database, deleting one member of a live trio, or claiming restore success before normal reopen.
  - Privacy text accurately states that conversation/tool/reasoning data is persisted in SQLite and user backups, credentials remain in SecretStorage, evidence/logs are redacted, and plain SQLite is not encrypted at rest; it makes no unsupported encryption, restore, migration, or cross-host synchronization claim.
- Tests first:
  - Add `src/task/sqlite/privacy-redaction.test.ts`: create a current database through `DbClient`/`SqliteTaskRepository`, generate and persist a non-literal canary as conversation content, create a W4 backup, independently reopen both, and assert the canary exists only in the intended content columns/artifact while metadata DTOs and enumerated error/diagnostic/command/evidence serializers do not contain it, paths, SQL, or stack text.
  - In the same suite, table-drive fixed faults across open/read/write/transaction/backup/reset-facing serialization and capture `debugMuster`/output/notification dependency seams where available; assert exact allowlisted keys/codes and canary absence rather than merely checking that calls occurred.
  - Extend `scripts/repository-boundary-smoke.test.mjs` and `scripts/source-boundary-smoke.test.mjs` with negative fixtures that add representative `debugMuster(...rawError)`, `appendLine(sql/params/path/content)`, telemetry/content fields, a legacy importer/`.muster-tasks.json` path, or raw live-main copy; each fixture must fail for its intended boundary rule while safe fixed fields and direct basename-only success UI remain accepted.
  - Add `scripts/verify-sqlite-storage-docs.test.mjs` with tracked-file marker assertions for location/scope/WAL, command IDs/titles from `package.json`, backup-versus-export, no in-product restore/import command, the positive all-windows-closed coordinated manual-restore procedure, reset/recovery flows, SecretStorage, and no encryption-at-rest claim. Add mutation fixtures that remove each required topic/manual-restore step or inject false encryption, open-database replacement, stale-sidecar reuse, auto-reset, partial-file-delete, migration, or export-as-backup claims and assert rejection.
- Anti-shortcut coverage:
  - Generate the canary at test runtime and persist it in more than one conversation-shaped field so literal filtering and one-column special cases fail; require positive presence in the independently reopened backup plus negative absence from structurally enumerated sinks. Boundary mutation fixtures reject comments-only assurances or guards that look only for the test canary, and docs mutation tests reject a page that merely lists command names without scope, failure, restore, and privacy semantics.
- Implementation obligations:
  - Inventory all current SQLite error, backup/reset, command notification, `debugMuster`/OutputChannel, UAT, and evidence call sites. Reuse fixed DTO constructors; if a test reveals a leak, remove the sensitive field at its producer rather than regex-scrubbing final strings. Do not redact intended user-visible conversation content in the actual transcript or backup.
  - Add narrowly targeted source/static rules over production sink call sites and UAT/evidence schemas, with self-tests proving both reject and accept behavior. Do not ban generic logging repo-wide, add a speculative logger abstraction, or use the generated canary value as a production filter.
  - Add the storage guide, docs index/root links, contributor commands, and package script `test:sqlite-storage-docs`; keep command identifiers/titles sourced from the real manifest/handler constants in assertions. The Phase 5 evidence script belongs to Phase 2, after its evidence schema exists.
  - Keep all backup/reset mechanics unchanged unless the audit proves a concrete leak. Preserve worker ownership, fixed error taxonomy, fail-closed behavior, direct-user-only basename display, and the no-telemetry/no-compatibility invariants.
  - Mark only P5-W6 complete in `docs/plans/sqlite-global-storage-refactor.vi.md` and update this plan's phase/progress row in the same commit; do not mark P5-W7, Batch C, or Phase 5 complete.
- Acceptance criteria:
  - [x] AC-1: A runtime-generated canary is present in intentional durable conversation content and an independently reopened user backup, and absent from every enumerated RPC/error/diagnostic/log/command/change-feed/snapshot-metadata/UAT-evidence surface - proven by `src/task/sqlite/privacy-redaction.test.ts`.
  - [x] AC-2: Raw exception, stack, SQL/params, path/URI, content, identity, telemetry, legacy JSON/import, and live-main-copy mutations fail the relevant source-boundary fixture while fixed redacted fields and basename-only direct UI pass - proven by boundary smoke self-tests and production scans.
  - [x] AC-3: The tracked storage guide and indexes state the real profile+authority scope, WAL unit, exact backup/reset commands, backup/export distinction, absence of an in-product restore/import command, the supported all-windows-closed coordinated manual-restore procedure, recovery actions, and plain-SQLite privacy limitation without false compatibility/encryption claims - proven by `scripts/verify-sqlite-storage-docs.test.mjs` positive and mutation tests.
  - [x] AC-4: Existing W1-W5 redaction, backup, reset, and command suites remain green with no production telemetry/content sink or behavior regression - proven by the focused and phase-gate commands.
- Focused verification:
  - `npx vitest run src/task/sqlite/privacy-redaction.test.ts src/task/sqlite/errors.test.ts src/task/sqlite/diagnostics.test.ts src/task/sqlite/protocol.test.ts src/task/sqlite/backup.test.ts src/task/sqlite/reset.test.ts src/host/sqlite-maintenance-commands.test.ts && npm run test:sqlite-storage-docs && npm run test:source-boundary:fixtures`
- Phase gates:
  - `npm run compile && npm run check:svelte && npm test && npm run test:source-boundary && npm run test:source-boundary:fixtures && npm run test:sqlite-storage-docs && git diff --exit-code 036d740 -- src/task/sqlite/schema.ts src/task/sqlite/schema-fingerprint.ts && git diff --check`
- Review: run `codex-impl-review` against this phase and this plan; verdict must be APPROVE.
- Commit: `docs: define sqlite privacy and recovery contract`

## Phase 2: P5-W7 packaged fault UAT and Phase 5 closeout
- Status: complete
- Depends on: Phase 1
- Goal: Exercise the real packaged worker and activated-host maintenance paths under the complete Phase 5 fault matrix on VS Code 1.101 and stable, publish strictly redacted evidence, rerun Phase 4 regression gates, and close Phase 5 truthfully.
- Current behavior: `scripts/sqlite-extension-host-smoke.ts:run` activates a fresh VSIX and verifies current schema plus happy-path backup only. The Phase 4 two-window UAT uses two real Extension Hosts but has no fault/reset scenarios. Full corrupt/write/backup/reset coverage exists only in source-level tests, and there is no Phase 5 packaged evidence schema or closeout ledger.
- Code evidence: `scripts/run-sqlite-extension-host-smoke.mjs:main`; `scripts/sqlite-extension-host-smoke.ts:run/PackagedDbClient`; `scripts/run-sqlite-two-window-live-uat.mjs:spawnWindow/linkSharedGlobalStorage/buildEvidence`; `scripts/sqlite-two-window-live-uat.ts:runOrchestratorA/servePeerB`; `src/host/uat-commands.ts:UAT_COMMANDS/isUatModeEnabled`; `src/extension.ts:registerLiveUatCommands/registerSqliteMaintenanceCommands`; `src/task/sqlite/fault-inject.ts:maybeInjectFault`; `src/task/sqlite/client.ts:DbClient`; `src/task/sqlite/backup.ts:backupOpenDatabase`; `src/task/sqlite/reset.ts:resetOpenDatabase`; `scripts/bench-phase4-release.ts`; `docs/plans/sqlite-phase4-gate-evidence.vi.md`.
- Pattern to follow: Build a fresh-VSIX runner using `run-sqlite-extension-host-smoke.mjs`; use the explicit non-production UAT gate and real activated repository pattern from `registerLiveUatCommands`; use the separate-user-data/shared-globalStorage process orchestration from the Phase 4 two-window runner; model structured/redacted evidence and negative verifier fixtures after `run-sqlite-two-window-live-uat.mjs:buildEvidence` and `verify-live-uat-evidence.test.mjs`.
- Behavioral contract:
  - One Batch C packaged-fault command runs the same required scenario IDs against VS Code 1.101.0 and current stable using freshly built VSIX contents. Each scenario reports only runtime/version, scenario ID, fixed result code, duration/count, and an optional bounded hash/size; failure exits non-zero and never writes a PASS ledger entry.
  - Packaged open scenarios create isolated corrupt/not-a-database, foreign-application, and Muster-owned incompatible fixtures; real packaged `DbClient.open` returns the expected fixed code, leaves rejected source ownership/schema/content unchanged, starts no activated engine for the fail-closed global-storage case, and keeps recovery commands registered.
  - Packaged injected `full` and `readonly` write scenarios execute representative repository transactions through an explicitly capability-enabled test worker, then independently reopen and prove rows/revision/feed remain at the pre-fault state with no success ACK. The fault control is available only under the existing non-production UAT gate and never through production settings or contributed commands.
  - A real second worker holds SQLite contention while the tested packaged worker waits/fails with fixed `busy`; an Extension Host timer/command heartbeat continues within its bounded threshold, and committed state remains readable afterward.
  - Backup runs while another worker commits data held in WAL. The W4 worker path returns one consistent pre/post revision, the independently read-only reopened artifact passes ownership/current version/fingerprint/`quick_check`, and source remains writable; no test copies the live main file.
  - Reset cancellation executes the same dependency-injected maintenance handler used by the contributed command with a deterministic UAT-only cancel choice and performs zero quiesce/reset/reload. Reset success uses the real maintenance hold/quiesce and worker reset path, verifies current empty schema/data and reload-request ordering through a test-only observer, and does not physically replace the live trio.
  - Cross-window reset contention uses two distinct packaged VS Code processes sharing one `globalStorage` database. A peer-held write transaction causes fixed `busy`/close-other-windows with original rows intact; after release, the successful exclusive reset is atomic and the stale peer follows the existing revision-regression hard-quiesce/reload behavior rather than writing old projection state or forming a second database lineage.
  - The evidence verifier requires all scenarios for both runtime classes, exact allowlisted keys/types/bounds, PASS fixed result codes, distinct-host proof for contention, and canary absence. It rejects absolute paths, URI/workspace/task/session identities, UAT control payloads or synthetic entity IDs, content, cursor, SQL, stack/raw exception, timestamps embedded as local paths, unknown fields, or a missing/duplicate scenario. The existing Phase 4 regression may continue using synthetic opaque IDs in its gated control/evidence contract; it is regression input, not the Phase 5 evidence format or a source for copied fields.
- Tests first:
  - Add `scripts/sqlite-phase5-evidence-schema.test.mjs` for the pure packaged-fault result/evidence validator using a complete synthetic matrix, then mutation cases for each missing runtime/scenario, duplicate scenario, FAIL result, unknown key, unbounded/free-form detail, path/URI/ID, SQL/stack, and secret-canary leakage; these tests run before any tracked evidence exists and must fail before the validator/schema is implemented.
  - Add/extend `src/host/uat-commands.test.ts` and maintenance command tests for the UAT-only reset cancel/success observer: production mode never registers/accepts it, cancel has zero side effects, success orders hold/quiesce/reset/verify/reload-request, and all returned fields are fixed/non-sensitive.
  - Add the packaged Extension Host scenario entrypoint tests for corrupt/foreign/incompatible fixtures, full/readonly rollback, busy heartbeat, and WAL-writer backup/reopen. Every fault case independently verifies durable rows/revision and exact safe error shape, not only process exit.
  - Add a dedicated two-process packaged reset-contention scenario using the Phase 4 launcher primitives: peer B holds/releases a real transaction, A first observes atomic busy failure and then successful reset, and B reports one hard-quiesce/reload signal with rejected stale writes. Assert shared physical token equality without storing the path.
  - Add `scripts/verify-sqlite-phase5-evidence.test.mjs` for the tracked evidence and `docs/plans/sqlite-phase5-gate-evidence.vi.md`; mutation tests must reject claiming Phase 5 complete when either runtime, any fault scenario, Phase 4 benchmark/live-UAT regression, gate command, or redaction declaration is absent.
- Anti-shortcut coverage:
  - Tests load client/worker modules from the extracted VSIX and validate the packaged path, so source-tree-only mocks fail. Independent reopen and pre/post revision oracles reject catch-and-label-PASS, reset-by-clear-one-workspace, raw-main backup, and rollback assertions based only on in-memory projection. A real peer transaction plus heartbeat rejects single-process fake contention and main-thread busy waits; the evidence validator rejects hand-authored free text, omitted failures, canary scrubbing after evidence generation, or one-runtime-only claims.
- Implementation obligations:
  - Add a focused packaged fault-UAT entrypoint/runner and package script `test:sqlite-packaged-fault-uat`; package once where practical, run the required 1.101.0/stable matrix deterministically, isolate all fixtures/user-data, enforce timeouts, close workers/processes, and clean temporary databases/artifacts even after failure.
  - Reuse `DbClient`, worker fault capability, backup/reset modules, maintenance handlers, and current activation. Any new UAT command must be exact-key validated, registered only when `isUatModeEnabled` is true in a non-production Extension Host, return no content/identity/path, and have a source-boundary rule proving it is not contributed or reachable in production.
  - Refactor maintenance dependency construction only as narrowly as needed so packaged cancel/success traverses the same handler/quiesce/reset behavior; do not add a production bypass for modal confirmation, maintenance hold, verification, or reload.
  - Serialize evidence from typed scenario results through one allowlist builder before writing `docs/plans/sqlite-phase5-packaged-fault-uat-evidence.json`; validate the in-memory shape before publication and never copy process logs wholesale. On failure retain diagnostics only in ignored temporary files and print fixed scenario/code context without the canary or local paths.
  - Follow this closeout order: run the packaged fault matrix first, then minimum/stable packaged smokes, Phase 4 live two-window UAT, and `bench:phase4-release:assert`; only after all succeed, record their actual runtime versions, scenario result codes, benchmark budget verdicts, and exact gate commands in `sqlite-phase5-gate-evidence.vi.md`, update completion markers, and finally run `test:sqlite-phase5-evidence` against the fresh tracked artifacts. Do not change Phase 4 budgets or claim a new benchmark.
  - Re-audit production source and extend boundaries where necessary for no silent reset, malformed-row skip, swallowed busy, raw live-WAL copy, legacy JSON/migration/backcompat, UAT leakage, or unbounded hydration. Do not implement unrelated cleanup.
  - Use one non-circular closeout sequence: after focused and behavioral/producer gates pass, write the fresh evidence and mark P5-W7, Batch C, P5-W1 through P5-W7, and Phase 5 complete in `docs/plans/sqlite-global-storage-refactor.vi.md`; update this plan's phase status, all acceptance boxes (including the pre-commit history/diff AC-6), and progress row (using `HEAD (P5-W7 commit)` rather than an unknowable pre-commit SHA); run the evidence verifier, canonical-schema check, and final diff checks against that complete uncommitted diff; then obtain `codex-impl-review` APPROVE and commit the entire W7 change once. Never mark completion before the producer gates pass or modify tracked markers after the approving review.
  - Treat final history/worktree validation as a read-only post-commit handoff, not a tracked pre-commit checkbox: after the W7 commit, run `git log --oneline 036d740..HEAD` and `git status --short`, report exactly one W6 commit followed by one W7 commit and no output from status, and do not amend or create a bookkeeping commit.
  - Preserve the canonical schema mechanically: do not edit `src/task/sqlite/schema.ts` or `src/task/sqlite/schema-fingerprint.ts`, and run the baseline diff gate before review/commit so packaged “current schema” assertions cannot follow a changed DDL/version/fingerprint.
- Acceptance criteria:
  - [x] AC-1: Freshly packaged VSIX runs on VS Code 1.101.0 and stable and reports PASS for corrupt/not-a-database, foreign/incompatible reject, injected full/readonly rollback, busy responsiveness, WAL-writer backup/reopen, reset cancel/success, and cross-window reset contention - proven by `npm run test:sqlite-packaged-fault-uat` and the tracked matrix evidence.
  - [x] AC-2: Every failure is fixed-code/redacted/fail-closed and independent reopen proves rejected opens do not mutate ownership, failed writes/resets preserve prior rows/revision, and no scenario auto-resets or silently loses rows - proven by packaged scenario assertions and evidence result codes.
  - [x] AC-3: Busy and reset contention use real separate workers/processes while host heartbeat remains responsive; successful reset preserves one physical DB identity and a stale peer hard-quiesces/reloads instead of writing - proven by the two-process packaged scenario's token, heartbeat, durable-state, and peer-state oracles.
  - [x] AC-4: Evidence contains exactly the allowlisted bounded fields for both runtimes and all scenarios, contains no canary/path/URI/content/IDs/cursor/SQL/stack/raw exception, and cannot claim completion with a missing or failed gate - proven by `scripts/verify-sqlite-phase5-evidence.test.mjs` positive and mutation tests.
  - [x] AC-5: Phase 4 benchmark budgets, existing packaged smoke, and live two-window UAT remain green without budget relaxation; full compile/Svelte/build/tests/boundaries/docs/evidence gates pass - proven by the phase-gate commands and Phase 5 gate ledger.
  - [x] AC-6: Immediately before the W7 commit, history since `036d740` contains exactly the completed W6 commit and the complete reviewed uncommitted diff contains only W7/closeout work with Phase 5 markers changed in that diff, not W6 - proven by `git log --oneline 036d740..HEAD`, `git diff --stat`, and marker inspection before review/commit. Final two-commit history and clean-worktree confirmation remain the read-only post-commit handoff obligation above.
  - [x] AC-7: Batch C leaves the schema version, canonical DDL, and schema fingerprint implementation exactly as they were at `036d740`, and packaged backup/reset verification still reports schema version 7 against that baseline - proven by `git diff --exit-code 036d740 -- src/task/sqlite/schema.ts src/task/sqlite/schema-fingerprint.ts` plus packaged scenario assertions.
- Focused verification:
  - `npx vitest run src/host/uat-commands.test.ts src/host/sqlite-maintenance-commands.test.ts src/task/sqlite/connection.test.ts src/task/sqlite/write-failure.test.ts src/task/sqlite/backup.test.ts src/task/sqlite/reset.test.ts src/task/sqlite/main-thread-nonblocking.test.ts && node --test scripts/sqlite-phase5-evidence-schema.test.mjs`
- Phase gates:
  - `npm run compile && npm run check:svelte && npm run vscode:prepublish && npm test && npm run test:source-boundary && npm run test:source-boundary:fixtures && npm run test:sqlite-storage-docs && npm run test:sqlite-packaged-fault-uat && MUSTER_VSCODE_VERSION=1.101.0 npm run test:sqlite-extension-host && MUSTER_VSCODE_VERSION=stable npm run test:sqlite-extension-host && npm run test:sqlite-two-window-live-uat && npm run bench:phase4-release:assert && npm run test:sqlite-phase5-evidence && git diff --exit-code 036d740 -- src/task/sqlite/schema.ts src/task/sqlite/schema-fingerprint.ts && git diff --check`
- Review: run `codex-impl-review` against this phase and this plan; verdict must be APPROVE.
- Commit: `test: close sqlite phase 5 fault gates`

## Completion Criteria
- [ ] Every phase is complete and committed exactly once.
- [ ] Every acceptance criterion is checked.
- [ ] All global gates pass on final HEAD.
- [ ] Final `codex-impl-review` verdict is APPROVE for the complete plan range.
- [ ] Worktree is clean apart from pre-existing unrelated changes.

## Progress Log
| Phase | Status | Commit | Verification | Review |
|---|---|---|---|---|
| 1 | complete | f86861e | focused + docs + boundaries + compile/svelte + full suite green | APPROVE |
| 2 | complete | HEAD (P5-W7 commit) | packaged fault UAT 1.101+stable 12/12; evidence validators | pending |
